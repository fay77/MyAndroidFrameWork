<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.fenggao.myandroidframework">

    <application
        android:name=".core.CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialDesign">
        <activity android:name=".sample.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".sample.ContentActivity"/>
        <activity android:name=".sample.HomeActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".sample.LoginActivity"/>
        <activity android:name=".sample.SampleListActivity"/>
        <activity android:name=".sample.SampleListActivity1"/>
    </application>

</manifest>